/// <reference types="cypress" />
/// <reference types="@testing-library/cypress" />

declare namespace Cypress {
  interface Chainable {
    // Burada özel komutlar için type tanımlamaları eklenebilir
  }
} 